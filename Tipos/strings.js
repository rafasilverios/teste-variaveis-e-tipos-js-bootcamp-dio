// strings são textos ou frases

const DIO = 'Digital Innovation One'; // declaração de uma constante, pois será sempre a mesma, independente do aluno

let firstName = 'Rafael';
let lastName = 'Silverio';

let fullName = `${firstName} ${lastName} é um aluno da plataforma ${DIO}.` // concatenação do texto que irá aparecer no console

console.log(fullName);