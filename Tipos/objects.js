// estrutura tipo "chave e valor"

let person = {
    name: 'Rafael',
    age: '33',
    cidade: 'Curitiba'
}

person = `O aluno ${person.name} tem ${person.age} anos e mora em ${person.cidade}.` // concatenando os valores do objeto.

console.log(person);
