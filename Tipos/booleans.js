// verdadeiro ou falso

let validation = 10

if (!validation === 10) { // utilizando o "não" lógico. 
    validation = "true";
}  else {
    validation = "false";
}
/* neste caso, retornará o valor contrário
"validation" é igual a 10 (true), porém, o "não" lógico retorna o contrário.
portanto, o resultado mostrado no console é "false"*/

console.log(validation);